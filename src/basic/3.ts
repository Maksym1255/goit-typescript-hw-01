let union: string | number;

union = "string"; // ok
union = 15; // ok
// union = true; //Error

// ========================================================

let literal: 'enable' | 'disable';

literal = 'enable'; // ok
literal = 'disable'; // ok
// literal = 'literal'; // Error